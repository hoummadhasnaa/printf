#include "main.h"

/**
 * _isdigit - check if input is a digit between 0-9
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 if not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

